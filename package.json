{
  "name": "vim-input",
  "version": "1.3.0",
  "scripts": {
    "build": "esbuild src/content.js --bundle --minify --outfile=dist/content.js",
    "publish": "web-ext sign --channel=unlisted --api-key=$API_KEY --api-secret=$API_SECRET"
  },
  "dependencies": {
    "@codemirror/state": "^6.0.0",
    "@codemirror/theme-one-dark": "^6.1.3",
    "@replit/codemirror-vim": "^6.0.0",
    "codemirror": "^6.0.2",
    "web-ext": "^10.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
